{
  "name": "taxpay-anchor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy:localnet": "anchor deploy --provider.cluster localnet",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "idl:generate": "anchor build && anchor idl parse --file programs/taxpay/src/lib.rs --out target/idl/taxpay.json"
  },
  "dependencies": {
    "@coral-xyz/anchor": "0.31.0"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
